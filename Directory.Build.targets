<Project>
    <!-- Strip TRACE and DEBUG symbols in Release builds -->
    <PropertyGroup Condition="'$(Configuration)' == 'Release'">
        <DefineConstants>RELEASE;XUNIT_NULLABLE</DefineConstants>
    </PropertyGroup>
</Project>